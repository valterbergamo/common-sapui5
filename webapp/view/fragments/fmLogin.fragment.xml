<core:FragmentDefinition 
      xmlns="sap.m"
      xmlns:f="sap.ui.layout.form"
      xmlns:core="sap.ui.core">

      <Dialog
		id="openDialogLogin"
		title="{i18n>loginTitle}">
		<content>
			<VBox class="sapUiSmallMargin" id="formContainerLogin" >
				<f:SimpleForm  editable="true" layout="ResponsiveGridLayout">
				<f:content>
					<Label text="Usuário" design="Bold" required="true"/>
					<Input id="Lifnr"/>
					
					<Label text="Senha" design="Bold" required="true"/>
					<Input type="Password" id="Password" submit="onLogin"/>
				</f:content>
			</f:SimpleForm>
			</VBox>
		</content>
		<beginButton>
			<Button tooltip="Login" icon="sap-icon://save" text="Login" type="Emphasized" press="onLogin"/>
		</beginButton>
		<endButton>
			<Button tooltip="Cancelar" icon="sap-icon://cancel" text="Cancelar" type="Reject" press="onCancelLogin"/>
		</endButton>
	</Dialog>
</core:FragmentDefinition>